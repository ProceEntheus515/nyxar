"""
Rutas HTTP del grafo para el dashboard; se montan desde el servicio API FastAPI.
Implementación: PROMPTS_V8 (endpoints específicos).
"""

from __future__ import annotations

# from fastapi import APIRouter
# router = APIRouter(prefix="/entity-graph", tags=["entity_graph"])
