# === NYXAR - Dependencias unificadas (desarrollo local) ===
# Este archivo combina TODOS los requirements de los microservicios
# para que el IDE (Pylance) y el desarrollo local funcionen sin problemas.
# En producción, cada servicio usa su propio requirements.txt via Docker.

# --- api ---
fastapi
uvicorn
motor
redis
prometheus-client
python-socketio
pydantic

# --- collector ---
watchdog

# --- enricher + misp_connector ---
httpx
schedule

# --- notifier ---
aiosmtplib
jinja2

# --- ai_analyst ---
anthropic

# --- simulator ---
faker

# --- ad_connector ---
ldap3

# --- tests (dev) ---
pytest
pytest-asyncio
fakeredis
