/**
 * I07 — Verificación del store Zustand del dashboard.
 *
 * La comprobación en runtime está en dashboard/src/App.jsx (solo import.meta.env.DEV):
 * a los 3 s agrupa en consola conexión WS, conteos de eventos, identidades, etc.
 *
 * Pasos: levantar la API, ejecutar en dashboard `npm run dev`, abrir la app y revisar la consola.
 */

console.log(
  '[verify_store] Inicia el dashboard en modo desarrollo y revisa el grupo "Verificación del Store (I07)" en la consola del navegador.',
)
